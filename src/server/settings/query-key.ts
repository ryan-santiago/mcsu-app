/** Shared between the server page (prefetch) and the client view (read/invalidate). */
export const myProfileQueryKey = ["settings", "my-profile"] as const;
export const myPendingRequestQueryKey = ["settings", "my-pending-request"] as const;
